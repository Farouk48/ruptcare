<div class="main-chatroom-app">
	<div class="sidebar-chatroom">
		<div class="sidebar-navigation">
			<ul>
				<li class="sidebar-navigation li"><i class="fas fa-inbox"></i> Inbox <span class="notif"> 2 </span></li>
				<li class="sidebar-navigation li"><i class="fas fa-pen"></i> Brouillons <span class="notif">  </span></li>
				<li class="sidebar-navigation li"><i class="far fa-paper-plane"></i></i> Envoyés <span class="notif"> 2 </span></li>
					<li class="sidebar-navigation li"><i class="fas fa-exclamation-triangle"></i></i> Urgent <span class="notif">  </span></li>
				<li class="sidebar-navigation li"><i class="fas fa-trash"></i></i> Corbeille <span class="notif">  </span></li>
			</ul>
		</div>

		<div class="sidebar-chatroom sidebar-navigation sidebar-conv-clean">
			<ul>
				<li class="sidebar-navigation li"><i class="fas fa-check"></i>Validées</li>
				<li class="sidebar-navigation li"><i class="far fa-clock"></i>En attente</li>
				<li class="sidebar-navigation li"><i class="fas fa-times"></i>Rejet</li>
			</ul>
		</div>

		<div class="sidebar-conv-doc">
			<div class=""
				<h2> Docteurs<span></span></h2>
				<button>Tout voir </button>
	    </div>



<h1>#<%= @chatroom.name %></h1>
<div id="messages" data-chatroom-id="<%= @chatroom.id %>">
  <% @chatroom.messages.each do |message| %>
    <%= render 'messages/message', message: message %>
  <% end %>
</div>

<%= simple_form_for [ @chatroom, @message ], remote: true do |f| %>
  <%= f.input :content, label: false, placeholder: "Message ##{@chatroom.name}" %>
  <%= f.button :submit %>
<% end %>
