<div id="title-pharma" class="container">
  <h1 >Réseau Rupt'Care</h1>
  <h2>Au <%= Date.today %> à <%= Time.now.to_s(:time) %>, les pharmacies suivantes du réseau disposent de la <strong>Mirtazapine 15 mg</strong> en stock :</h2>
</div>

<div id="map-pharma">
  <% @pharmas.each do|pharma| %>
  <div>
    <h2><%= pharma.name %></h2>
    <p><%= pharma.address %></p>
 </div>
  <% end %>

  <div id="map"
       style="width: 50%; height: 600px;"
       data-markers="<%= @markers.to_json %>"
       data-mapbox-api-key="<%= ENV['MAPBOX_API_KEY'] %>">
  </div>
</div>
